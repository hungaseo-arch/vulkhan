# 변경 이력

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고, 날짜는 작업일 기준입니다.
2026-08-12 이전의 상세 작업 기록은 `doc/CHANGELOG.md`(git 미추적)에 있습니다.

## [Unreleased] — 2026-09-11

계정 관리 보강, 전표 상태 정정, 고객별 여신한도·결제조건 결재, 삭제 결재, 고객 상세항목,
기초재고 입력을 포함합니다.

### Added
- **비밀번호 초기화(관리자).** 사용자 목록의 `Reset Sandi` 버튼 → `POST /api/pengguna/:id/reset-sandi`.
  초기 비밀번호는 `ascendo123`이며 관리자 화면에 그대로 표시됩니다. 값은 `api-server.js`의 `SANDI_AWAL`과
  `src/App.jsx`의 동명 상수 두 곳에 있으니 바꿀 때는 함께 바꿔야 합니다. 본인 계정은 대상에서 제외되고
  (기존 "비밀번호 변경" 메뉴를 쓰도록) 확인 대화상자에 대상 아이디와 초기 비밀번호가 함께 뜹니다.
- **계정 수정.** 사용자 목록에서 행을 클릭 → `PUT /api/pengguna/:id`. 이름과 역할만 바꿀 수 있고
  아이디·비밀번호는 대상이 아닙니다. 잠김 방지 장치 두 개: 본인 역할 변경 불가, 마지막 admin의 강등 불가.
- **완납/미납 정정.** 판매·구매 전표 상태를 한 단계 **되돌리는** `←` 버튼. `langkahBerikut`를
  `pindahStatus`로 바꿔 한 단계 전진 또는 한 단계 후퇴를 허용하되, 재고가 실제로 움직이는 단계
  (판매 `kirim`, 구매 `diterima`) **아래로는 내려가지 않습니다** — DB 트리거가 만든 `stok_mutasi`가
  고아로 남지 않도록. 출고 자체를 되돌리려면 전표를 삭제해야 합니다.
- **고객별 여신한도 결재(담당자 기안 → 관리자 확정).** 새 테이블 `limit_usulan`과
  `GET/POST /api/limit-usulan`, `POST /api/limit-usulan/:id/putusan`. 고객 화면에 기안 목록 카드와
  `Usul Limit` 버튼이 생기고, `pelanggan.limit_kredit`는 관리자가 승인한 순간에만 바뀝니다.
  승인은 "대기 중일 때만 표시 → 표시된 행에서 금액을 읽어 반영"을 한 트랜잭션으로 묶어 동시 확정에도
  한도가 어긋나지 않습니다. 한 고객에 대기 중인 기안은 하나뿐(부분 유니크 인덱스).
  마이그레이션 기록은 [migrations/2026-09-11-limit-usulan/](migrations/2026-09-11-limit-usulan/).
- **결제조건(TOP)도 같은 기안으로 결재.** 기안 화면에 `신규 결제조건(일)`이 생겨 여신한도와 결제조건을
  한 번에 올립니다(`limit_usulan.termin_lama` / `termin_baru`). 둘 중 하나만 달라도 기안은 성립하고,
  모두 현재와 같으면 거부합니다. 일 단위라 정수만 받습니다 — 소수 일수는 인보이스에서 뜻이 없고
  미수금 연령 계산을 어긋나게 합니다. 옛 기안은 두 열이 NULL이며 "TOP과 무관"을 뜻하므로,
  승인은 `termin = COALESCE(u.termin_baru, p.termin)`로 반영해 옛 기안을 승인해도 현재 결제조건이
  지워지지 않습니다.
- **삭제 결재(담당자 기안 → 관리자 승인 시 삭제 실행).** 새 테이블 `hapus_usulan`과
  `GET/POST /api/hapus-usulan`, `POST /api/hapus-usulan/:id/putusan`. 판매·구매 전표를 지우면 그 전표가
  만든 `stok_mutasi`도 함께 사라지므로, 삭제 버튼 한 번이 창고 잔고를 기록 없이 움직였습니다.
  **직접 삭제는 admin만** 할 수 있게 `DELETE /api/{penjualan,pembelian,pelanggan}/:id`의 요구 권한을
  manager → admin으로 올리고, 나머지 역할에는 `Usul Hapus` 버튼을 줬습니다. 판매 상세 대화상자,
  구매 목록, 고객 목록 세 곳에 붙고 각 화면 위에 대기 건 카드가 뜹니다. 대기 중인 요청이 있는 행은
  버튼이 `결재 대기`로 바뀌어 눌리지 않습니다(한 대상에 대기 건은 하나 — 부분 유니크 인덱스).
  여신한도 결재와 달리 승인 트랜잭션은 **삭제를 먼저 실행하고** `status='disetujui'` 표시를
  `NOT EXISTS(대상 행)`에 겁니다. 삭제가 막히면 UPDATE가 아무 행도 잡지 못해 **승인이 기록되지 않고**
  409로 끝납니다 — 실제로 일어나지 않은 일을 "승인됨"으로 적는 것보다 눈에 보이는 실패가 낫습니다.
  대상 행을 FK로 참조하지 않는 것도 같은 이유입니다(`ON DELETE CASCADE`면 결재 기록이 같이 지워지고,
  일반 FK면 삭제가 실패). 대신 전표번호·설명·금액을 기안 시점에 **서버가 직접 읽어** 복사해 둡니다 —
  원본이 사라진 뒤 "무엇을 지웠는가"에 답할 수 있는 건 그 사본뿐이고, 삭제를 요청한 쪽이 보낸 설명을
  그대로 믿을 수는 없기 때문입니다. 삭제 사유는 필수 입력입니다.
  마이그레이션 기록은 [migrations/2026-09-11-hapus-usulan/](migrations/2026-09-11-hapus-usulan/).
- **고객 상세항목(인보이스 기재사항 + 영업 항목).** `pelanggan`에 `pemilik`(오너), `alamat`(주소),
  `email`, `npwp`(사업자번호), `sales`(영업담당), `catatan`(내부 메모) 6개 열을 더하고
  `PUT /api/pelanggan/:id`로 수정할 수 있게 했습니다. **여신한도와 결제조건은 이 엔드포인트의 대상이
  아닙니다** — 결재를 우회해 직접 고칠 수 있으면 기안 절차 자체가 무의미해지므로.
  주소와 사업자번호는 판매 인보이스에도 함께 인쇄되고, 검색·엑셀 내보내기에도 새 항목이 들어갑니다.
  마이그레이션 기록은 [migrations/2026-09-11-pelanggan-profil/](migrations/2026-09-11-pelanggan-profil/).
- **미수금 화면에서 상세로 바로 이동.** 위험 고객·인보이스 상세의 **고객명**은 고객 상세 대화상자를,
  **인보이스 번호**는 판매 상세 대화상자를 엽니다. 연령분석 표는 **건이 있는 행을 클릭**하면 그 구간에
  속한 인보이스 목록이 뜨고, 거기서 다시 개별 전표로 들어갑니다. 빈 구간은 열리지 않습니다.
  미수금 화면의 고객 대화상자에는 `여신한도 기안` 버튼을 두지 않았습니다 — 기안은 대기 건 목록이
  보이는 고객 화면에서 해야 중복 기안을 미리 막을 수 있습니다.
- **판매 내역 셀을 클릭하면 상세 대화상자**가 열립니다. 목록에는 품목 코드와 수량만 보이지만
  여기서는 단가·행별 금액·합계와 고객/창고/결제조건/만기일까지 함께 읽힙니다. 인쇄용 문서는
  고객에게 나가는 서식이라 담당자가 숫자를 확인하는 용도로는 무거워 따로 두었습니다.

- **기초재고 일괄 입력.** 창고 재고 탭의 `기초재고` 버튼. 창고 하나를 골라 품목별 개시 수량을 한 화면에서
  입력합니다(`GET/POST /api/mutasi/saldo-awal`, 저장은 manager 이상). 입력값은 **개시 시점의 재고**이며
  현재 재고가 아닙니다 — 현재 재고 = 기초재고 + 이후의 모든 입고·출고이므로, 지금 수량 0으로 두고
  나중에 실제 수량을 넣어도 그 사이에 기록된 입·출고가 사라지지 않습니다.
  스키마 변경 없이 `stok_mutasi`에 `tipe='penyesuaian'`, `ref='AWAL'` 한 줄로 기록하므로 `v_stok`이
  그대로 합산합니다. 창고·품목당 기초재고 행은 하나뿐이고 다시 저장하면 **교체**되므로(더해지지 않음)
  오타는 다시 저장해 고치면 됩니다. 재고 장부에서는 `기초재고`로 따로 표시됩니다.
  `현재 재고를 0으로 맞추기` 버튼은 선택한 창고의 모든 품목에 대해 `기초재고 = 기존 기초재고 − 현재 재고`를
  채워 넣습니다. 기초재고 없이 출고만 기록돼 재고가 음수로 내려간 상태를 한 번에 정리하기 위한 것으로,
  값은 입력칸에 채워질 뿐이라 저장 전에 확인·수정할 수 있습니다. 이 용도 때문에 기초재고는 **음수도 허용**합니다
  (이미 양수인 재고를 0으로 내리려면 음수가 필요). 나중에 실제 기초재고를 입력하면 이 보정값은 교체되므로
  이중 계상이 생기지 않습니다.
- 사용자 목록에서 **행을 클릭하면 수정 대화상자**가 열립니다. 별도의 `수정` 버튼은 없애고 작업 열에는
  `비밀번호 초기화`·`삭제`만 남겼습니다(버튼 클릭은 행 클릭으로 번지지 않습니다).
- 고객 목록도 같은 방식으로 **행 전체가 상세 대화상자**를 엽니다(이름만 누를 수 있던 것에서 바뀜).
  상세 대화상자 아래에 `고객 정보 수정`과 `여신한도 기안` 버튼이 있어 조회에서 바로 이어집니다.

### Fixed
- **판매·구매 탭이 백지로 뜨던 문제.** 두 컴포넌트가 `useLang()`에서 `t`만 꺼내 쓰면서 `lang`을
  참조해(`tglPanjang(tgl, lang)`, `bulanLabel(b.bulan, lang)`) 렌더 도중 `ReferenceError`가 났습니다.
  전표가 한 건이라도 있으면 즉시 터지고, 오류 경계가 없어 앱 전체가 하얗게 변했습니다.
  `e21c53a`에서 들어와 배포된 채로 남아 있던 버그입니다.
  재발 방지로 oxlint에 `no-undef`를 켰습니다(`env: browser + node + es2024`) — 버그를 되돌려
  실제로 검출되는지 확인했습니다. 같은 실수를 파일 전체에서 훑어 이 두 곳뿐임도 확인했습니다.
- 언어를 인도네시아어로 두어도 날짜 입력칸이 비어 있을 때 한국어 `연도. 월. 일.`이 보이던 문제.
  크롬은 `<input type="date">`의 빈 칸 안내문을 **브라우저 UI 언어**에서 가져오므로 `lang` 속성으로는
  바꿀 수 없습니다. 새 `Tgl` 컴포넌트가 값이 없을 때 `::-webkit-datetime-edit`를 감추고 앱 언어로 된
  안내문(`hh/bb/tttt` / `연도. 월. 일.`)을 겹쳐 그립니다.

### Changed
- **판매 화면 재설계 (VLK-UI-2026-001).** 필터 → KPI → 추이 차트 → 드릴다운 표의 3단 수직 구조로 바꿨습니다.
  - **기간 프리셋 칩**(`이번 달`/`지난달`/`분기`/`올해`/`직접 입력`)이 빈 날짜 입력칸 두 개를 대신합니다.
    기본값은 `올해`입니다 — 범위가 비어 있는 화면은 열었을 때 아무것도 답해주지 않습니다. 선택 상태는
    `?period=ytd&q=3&cari=…` 형태로 URL에 기록되어 새로고침·공유에도 남습니다(`history.replaceState`).
  - **KPI 4종**(매출액·판매수량·평균단가·거래건수)에 **전년 동기 대비 증감률**을 붙였습니다.
    `Rp 3.00 miliar`는 작년이 얼마였는지 알기 전까지 좋은 숫자인지 나쁜 숫자인지 판단할 수 없습니다.
    평균단가는 매출·수량과 **같은 방향**으로 색을 씁니다 — 단가 하락은 중립이 아니라 나쁜 신호입니다.
    ±0.5% 이내는 회색(보합)으로 둡니다. 전년 데이터가 없으면 빈칸이 아니라 `전년 데이터 없음`을 씁니다.
  - **월별 추이 차트**(막대 = 매출액, 보조선 = 평균단가). 차트 라이브러리를 **새로 넣지 않고** HTML 막대 +
    SVG `polyline` 하나로 직접 그렸습니다 — 막대 12개와 선 하나에 150 kB를 더할 이유가 없습니다.
    막대 높이는 % 가 아니라 픽셀로 계산합니다: 위에 겹치는 SVG가 같은 좌표계를 써야 선과 막대가 어긋나지
    않습니다. 미도래 월은 막대를 그리지 않고 축 라벨만 흐리게 둬서 **0건인 달과 구분**합니다.
    막대를 클릭하면 아래 표의 해당 월이 펼쳐지며 그 행으로 스크롤됩니다.
  - **분기 4열 그리드 폐지.** 분기는 표 안의 소계 구분행이 되었습니다. 모든 펼침이 **표 폭 안에서** 일어나므로
    월을 열어도 레이아웃이 바뀌지 않습니다 — 예전 구조는 월 하나를 열면 행렬에서 빠져나와야 했습니다.
    깊이는 색이 아니라 **들여쓰기**(월 16px · 일 34px · 거래 52px)로 읽힙니다. 서로 가까운 회색 세 단계는
    순서로 읽히지 않습니다.
  - **평균단가·전월비 열 신설.** 평균단가는 행별 단가의 산술평균이 아니라 **총액 ÷ 총수량**(가중평균)입니다 —
    1 pcs짜리 행과 200 pcs짜리 행을 같은 무게로 두면 실제로 받은 단가가 나오지 않습니다.
    전월비는 **날짜 필터 밖의 원장**에서 전월 값을 읽습니다. 그렇지 않으면 기간의 첫 달은 언제나 `—`가 됩니다.
  - **금액 표기 `juta`/`miliar` 축약**(소수점 둘째 자리). `Rp 3,001,976,000`은 자릿수를 세어야 크기를 알 수
    있지만 `Rp 3.00 miliar`는 그렇지 않습니다. 100만 미만은 축약하지 않습니다(`Rp 850,000`) —
    `Rp 0.85 juta`가 더 읽기 어렵기 때문입니다. **Excel·인쇄는 전체 자릿수를 유지**합니다.
  - **판매 표의 작업 버튼**(`→ 다음 상태` / `← 되돌리기` / `인쇄` / `삭제`)이 **판매 상세 대화상자로 이동**했습니다.
    새 6열 구성에 작업 열 자리가 없고, 기능을 없앨 수는 없습니다. 로직은 그대로이고 호출 위치만 옮겼습니다.
  - 색은 별도 토큰 `--pl-*` 5종으로 선언했습니다. 파스텔 3색은 **배경 전용**입니다 — 글자색으로 쓰면 명도
    대비가 WCAG AA에 못 미칩니다. 텍스트·경계선은 `#546E7A` 하나로 통일했습니다.
  - **분석 기준 3축**(`기간별` / `고객별` / `품목별`)을 KPI 아래 칩으로 두었습니다. 필터와 KPI 4종은
    세 축이 **공유**하고 바뀌는 것은 행을 묶는 방법뿐입니다 — 축을 바꿀 때 숫자가 다시 계산되는 것처럼
    보이면 어느 쪽이 맞는지 확인하느라 시간이 듭니다. 선택한 축은 `?sumbu=pelanggan` 으로 URL에 남습니다.
    월별 추이 차트는 **기간별에서만** 보입니다 — 차트의 가로축이 곧 시간축이라 다른 축에서는 뜻이 없고,
    그 자리를 비중 막대가 대신합니다.
  - **고객별: 매출 순위.** 순위·고객·거래건수·수량·매출액·평균단가·비중·**미수금** 8열. 기본은 상위 10건이고
    `전체 보기`로 펼칩니다. 고객명은 고객 상세를, **미수금 금액은 그 고객의 미결제 인보이스 목록**을 엽니다
    (미수금 화면과 **같은 `hitungPiutang` 계산**을 씁니다 — 화면마다 다른 숫자가 나오면 안 됩니다).
    행 왼쪽 `▸`를 누르면 그 고객의 해당 기간 전표가 펼쳐집니다. 미수금은 행마다 `piutang(id)`를 부르지 않고
    **한 번에 집계**합니다 — 행 수만큼 원장을 훑는 일을 피하려고.
  - **고객별: 신규·이탈 고객.** 신규는 **원장 전체에서의 첫 거래일**이 이 기간 안에 드는 고객이고,
    이탈은 **전년 동기에는 거래가 있었으나 이번 기간에는 없는** 고객입니다(마지막 거래일과 경과 일수 표시).
    두 목록 모두 시작일과 종료일이 **모두 정해져야** 나옵니다 — 하한이 없으면 모든 고객이 "신규"가 됩니다.
  - **품목별: 매출 순위**와 `품목` / `규격` / `패턴` **묶음 전환**. 집계는 SO가 아니라 **품목 행**에서 하고,
    거래건수는 품목 행 수가 아니라 **SO 단위**로 셉니다 — 한 전표에 세 품목이 있으면 세 묶음 모두에
    그 전표 하나가 잡혀야 합니다. 제품 마스터에는 **브랜드 열이 없어** 작업지시서의 "규격·브랜드"는
    `ukuran`(규격)과 `pola`(패턴)로 대신했습니다. 브랜드가 실제로 필요하면 스키마 변경이 먼저입니다.
  - Excel 내보내기는 **보고 있는 축을 따라갑니다** — 고객별/품목별에서는 순위표가, 기간별에서는 전표 목록이
    나옵니다. 상위 10건이 아니라 **전체 행**을 내보내고, 축약 없이 원 숫자를 씁니다.
- **SO 번호를 어디서든 클릭하면 판매 상세가 열립니다.** 판매 목록과 고객 상세의 거래 내역 표에서도
  전표 번호 자체가 링크입니다 — 미수금 화면에서만 되던 동작을 SO 번호가 보이는 모든 표로 맞췄습니다.
  번호는 화면마다 가장 먼저 읽는 칸인데 판매 목록에서는 `내역` 칸을 찾아 눌러야 했고, 고객 상세에서는
  아예 들어갈 길이 없었습니다. 고객 상세에서 전표를 고르면 **고객 대화상자는 닫고** 판매 상세를 엽니다
  — 연령분석 → 인보이스 목록 → 전표와 같은 방식으로, 대화상자를 겹쳐 쌓으면 `Esc`가 어느 쪽을 닫는지
  알 수 없어집니다. `DetailPelanggan`은 `onPilihSO`를 받은 화면에서만 번호를 버튼으로 그립니다.
- **판매 목록을 월 → 일 2단 아코디언으로.** 바깥이 월, 안이 일, 그 안이 전표입니다. 일별 카드만 늘어놓으면
  1년치가 쌓였을 때 카드가 수백 개가 되지만, 월로 한 겹 감싸면 화면 길이가 월 수만큼으로 고정됩니다.
  월 머리글과 일 머리글은 **같은 지표**(건수 · 수량 · 금액)를 보여줘서 펼치지 않고도 월별 비교가 됩니다.
  처음 열면 **가장 최근 월만 펼쳐진 상태**입니다 — 전부 접힌 화면은 아무것도 알려주지 않고, 찾는 것은
  대개 최근 월이므로. 펼침 상태는 `null`(아직 아무것도 누르지 않음)과 `Set`을 구분해 두었습니다:
  판매 데이터는 첫 렌더 이후에 도착하므로 기본값을 한 번만 계산해 담아두면 영영 비어 있게 됩니다.
  월 카드는 연도별로 묶여 **분기 4열 × 3행의 12개월 행렬**로 깔립니다. 열 = 분기, 행 = 그 분기의 몇 번째 달이고,
  각 칸의 위치를 CSS로 못 박아 두었기 때문에(`k1`~`k4` / `b1`~`b3`) **거래가 없는 달은 빈칸으로 남습니다** —
  뒤 칸이 앞으로 당겨지면 같은 열이 더 이상 같은 분기를 뜻하지 않게 되므로. 열 머리에는 분기 합계가 붙어
  분기 비교가 펼치지 않고도 됩니다. **펼친 월은 행렬에서 빠져 아래 한 줄 전체를 차지합니다**
  (`grid-column:1 / -1`) — 화면 1/4 폭에 전표 표를 넣으면 읽을 수가 없으므로.
  1180px 이하에서는 분기 행렬을 풀고 2열(720px 이하 1열)로 흘려보냅니다 — 좁은 화면에서 분기를 뜻하지
  않는 열은 분기 열이 아예 없는 것보다 더 헷갈리기 때문입니다.
  일 머리글은 `2026년 7월 30일`이 아니라 `30일 (목)`입니다 — 연·월은 바로 위 월 머리글에 이미 있고,
  요일은 판매 추이를 볼 때 실제로 쓰이는 정보입니다.
- **언어 전환 버튼을 국기로.** `ID` / `KO` 글자 대신 🇮🇩 / 🇰🇷 를 씁니다 — 읽지 않고 눈에 띄므로.
  선택되지 않은 쪽은 회색조(`filter:grayscale(1)`)로 낮췄습니다: 색이 살아 있는 국기 둘이 나란히 있으면
  어느 쪽이 켜져 있는지 배경 색조만으로는 구별이 잘 안 됩니다. 국기 이모지는 브라우저·스크린리더마다
  읽는 방식이 제각각이라 `aria-hidden`으로 감추고 버튼에 `aria-label`(`Bahasa Indonesia` / `한국어`)을
  붙였습니다. 국기 문자는 `src/i18n.jsx`의 `LANGS` 네 번째 항목입니다.
- **한국어 문구를 전부 개조식으로 전환.** 안내문·오류·토스트 89개에서 `…했습니다 / …하세요` 종결어미를
  없애고 명사형으로 바꿨습니다(`비밀번호가 변경되었습니다.` → `비밀번호 변경 완료`,
  `창고를 먼저 선택하세요.` → `창고 선택 필요`). 두 갈래는 일부러 남겼습니다 —
  삭제·초기화 확인 대화상자 5개는 되묻는 말이라 `— 진행?`으로 끝나고, 날짜 입력칸 안내문
  `연도. 월. 일.`은 문장이 아니라 자리표시자입니다. 인도네시아어 원문은 사전 **키**이므로 손대지 않았습니다
  (바꾸면 모든 `t()` 호출부가 깨집니다).
- `npm run api` 스크립트 추가 — `node --env-file=.env.local api-server.js`. 연결 문자열을 셸 명령줄에
  직접 쓰면 zsh가 큰따옴표 안의 `$$`를 PID로 치환해 비밀번호가 조용히 바뀌고
  `password authentication failed`가 납니다. 파일로 넘기면 셸을 거치지 않아 이 사고가 없습니다
  (단, `.env.local` 안에서도 `#`가 잘리지 않도록 값은 작은따옴표로 감싸야 합니다).
- 로그인 화면의 `VULKANISIR` 워드마크를 시그니처 이미지 기준으로 가운데 정렬
  (`letter-spacing` 때문에 생기는 오른쪽 여백을 `padding-left`로 보정).

## [Unreleased] — 2026-09-06

성능·정확성 점검, 아센도 BI 적용, 인도네시아어/한국어 전환을 포함합니다.

### Added
- **언어 전환(인도네시아어 / 한국어).** 헤더와 로그인 화면의 `ID / KO` 버튼. 선택은 `localStorage["vk_lang"]`에
  저장되고 `<html lang>`도 함께 바뀝니다. 번역 계층은 새 파일 [src/i18n.jsx](src/i18n.jsx)이며,
  **사전 키가 인도네시아어 원문**이라 번역이 없는 문자열은 인도네시아어로 그대로 표시됩니다.
  탭·대시보드·재고·판매·구매·고객·미수금·사용자 관리의 모든 화면 문자열 265개, 모달·토스트·오류 메시지,
  엑셀 내보내기 머리글이 대상입니다. 날짜도 언어에 따라 `2026년 9월 6일` / `6 September 2026`로 갈립니다.
  같은 인도네시아어가 뜻이 다른 경우는 `"키|문맥"`으로 구분합니다(`Masuk` 입고 / `Masuk|login` 로그인,
  `Keluar|mutasi` 출고 / `Keluar` 로그아웃, `Penawaran` 견적 / `Penawaran|dok` 견적서).
- 아센도 로고 적용. 헤더는 심볼, 로그인 화면은 시그니처, 파비콘·애플 터치 아이콘·`og:image`도 교체
  (`public/ascendo-symbol.png`, `public/ascendo-signature.png`).

### Fixed
- 오늘 날짜가 `"2026-07-23"` 상수로 고정되어 신규 판매·구매 전표의 기본 날짜, 전표 번호 접두(`SO-2607-…`),
  엑셀 파일명이 모두 7월로 나오던 문제. `today()` 함수로 교체해 항상 실행 시점의 로컬 날짜를 씁니다.
- 온라인 상태에서 재고를 "서버가 보낸 최근 500건 mutasi"의 합으로 계산하던 구조. 원장이 500건을 넘으면
  재고가 조용히 틀어지는 위험이 있어(현재 394건), 재고 원천을 서버 `v_stok` 뷰로 전환했습니다.
  mutasi 목록은 표시용으로만 쓰이고, 500건에 도달하면 화면에 안내 문구가 표시됩니다.
- 여러 문장으로 이루어진 쓰기(창고 이동 2건 INSERT, 전표 생성 header+items, 전표 삭제 3건 DELETE)가
  트랜잭션 없이 순차 실행되어 중간 실패 시 반쪽 데이터가 남을 수 있던 문제. Neon HTTP 드라이버의
  `sql.transaction()`으로 묶었습니다. 전표 생성 시 항목이 없으면 400을 돌려줍니다.
- 원장 목록 정렬이 온라인(오래된 순)과 오프라인(최신 순)에서 달랐던 문제. 서버가 최근 500건을 오름차순으로
  보내고 클라이언트가 뒤집어 항상 최신 순으로 표시합니다.

### Changed
- 색·서체를 아센도 BI로 개편. primary `#0062C6`, dark `#333333`, 본문 배경 `#f5f5f5`,
  `--asm-*` 토큰 도입(기존 `--slab`/`--paper`/`--ink` 이름은 별칭으로 유지).
- 서체를 Archivo Narrow + IBM Plex Sans/Mono에서 Noto Sans KR + Noto Sans로 교체. 한국어 UI에
  한글 글리프가 필요해서이며, 아래 "남은 과제"의 폰트 CSS 항목을 함께 보세요.
- 헤더 우측의 컨트롤 3개(사용자 이름 · 역할 배지 · 로그아웃 버튼)를 사용자 메뉴 하나로 통합.
  이름과 역할 색 아바타가 보이는 버튼을 누르면 비밀번호 변경 · 로그아웃이 드롭다운으로 열립니다.
  바깥 클릭과 ESC로 닫힙니다.
- KPI 금액이 두 줄로 접히던 문제. 글자 크기를 26px에서 24px로 줄이고 줄바꿈을 막았으며,
  좁은 카드에서는 `cqi` 기반으로 더 줄어들어 항상 한 줄을 유지합니다.
- 초기 로드와 매 쓰기 후 재로드가 엔드포인트 7개를 동시에 호출하던 것을 `GET /api/bootstrap` 한 번으로
  통합. Vercel에서 서버리스 함수 호출이 최대 7회에서 1회로 줄고, 쿼리는 서버에서 병렬 실행됩니다.
  기존 개별 엔드포인트는 그대로 유지됩니다.
- 판매·구매 목록에서 항목을 헤더에 붙일 때 헤더마다 `filter`하던 것을 `Map` 그룹핑으로 교체.
- 대시보드 월별 판매 표에 합계·월평균 행 추가, 창고별 음수 재고를 붉게 강조.
- 재고 원장 목록을 50건 단위 "더 보기"로 페이지네이션.
- `npm run deploy` 스크립트 추가 (lint → build → `vercel --prod`).
- 판매·구매 날짜 필터 입력에 `lang="id-ID"` 지정, 엑셀 내보내기에 PIC 열 추가.

### Performance
- Google Fonts `@import`가 React 컴포넌트 안 `<style>`에 있어 HTML → JS → 렌더 → 폰트 CSS → 폰트 파일의
  직렬 체인을 만들던 것을 `index.html`의 `preconnect` + 비차단 `preload` 링크로 이동.
- UI에 한글이 없는데 불러오던 IBM Plex Sans KR(폰트 CSS 40KB, @font-face 282개)을 IBM Plex Sans로 교체.
  폰트 CSS가 40KB → 1.5KB로 줄었습니다. **단, 이후 한국어 UI 도입으로 되돌렸습니다** — 위 Changed의
  Noto Sans KR 항목과 아래 "남은 과제"를 보세요.

### Docs
- 루트에 README.md, CONTRIBUTING.md, CHANGELOG.md, ARCHITECTURE.md 신설.
- 네 문서를 현재 코드에 맞춰 갱신: 다국어 규칙과 로고 사용 규칙(CONTRIBUTING), 다국어·브랜딩 구조와
  실제 서체(ARCHITECTURE), 기능·구조 목록(README). ARCHITECTURE의 서체 설명이 index.html과
  달랐던 것과 번들 크기(298KB → 322KB)를 바로잡았습니다.

### 남은 과제
- 구매 원장(`pembelian`)은 적재 계획이 없습니다. 개시재고 없이 판매 출고만 적재되어 있어 대시보드의
  음수 재고 경고는 실제 상태이며, 재고 현황은 추후 별도로 갱신합니다.
- **폰트 CSS가 393KB(gzip 92KB), `@font-face` 528개**입니다. Noto Sans KR의 `unicode-range` 서브셋이
  많아서이며, 이전 IBM Plex Sans 조합의 1.5KB에서 크게 늘었습니다. 실제 폰트 파일은 필요한 서브셋만
  받지만 CSS 자체가 큽니다. 한글 서브셋 self-host, 또는 `lang === "ko"`일 때만 Noto Sans KR을
  붙이는 방식이 후보입니다.
- 인쇄용 faktur·penawaran 서류 본문은 의도적으로 인도네시아어 고정입니다. 한국어 서류가 필요해지면
  `PENERBIT` 발행처 표기와 함께 별도로 설계해야 합니다.
- `cById`, `totalSO` 등 조회 헬퍼가 App 렌더마다 재생성되어 하위 `useMemo`가 매번 재계산됩니다.
- 성능·접근성 지표 재측정 필요. 위 Lighthouse 수치(성능 99 / 접근성 89)는 BI 개편과 다국어 적용
  **이전** 값입니다. 접근성 89의 근거로 적었던 명암비 색(`#71766F`, `#B45A1E`)은 BI 팔레트 교체로
  코드에서 사라졌고 `<main>` 랜드마크는 당시에도 있었으므로, 원인 항목부터 다시 확인해야 합니다.
- 약 26KB CSS가 JS 문자열 안에 있고, react/react-dom이 앱 코드와 한 청크에 묶여 있습니다.

## 2026-08-13
- SPB v2 적재 분석 문서와 초안 스크립트를 `migrations/2026-08-13-spb-v2/`에 보관. 초안 SQL은 실행 금지.

## 2026-08-12
- 창고 구조를 `UTM` 단일에서 KRW/SMG/SBY 3창고로 재편하고, SPB Destination 기준으로 판매 227건·원장 298건의
  창고를 재지정 (`migrations/2026-08-12-gudang-restructure`, `-destinasi-gudang-fix`).
- `produk."hargaUser"`(최종 사용자 가격) 컬럼 추가 및 백필.
- 2026년 1~7월 SPB 판매 394라인/290전표 적재 (`migrations/2026-08-12-may-backfill`).
- 생성 폼의 드롭다운을 검색 가능한 콤보박스로 교체, 첫 항목 자동 선택 대신 명시적 선택 요구.
- 창고 선택기가 존재하지 않는 id를 기본값으로 쓰던 폼 4곳 수정.
- 저장소를 배포본과 동기화: 인증 강화, 관리자 복구(`ADMIN_USER`/`ADMIN_PASS`), 상태 전이 한 단계 제한.

## 2026-07-24
- admin의 사용자 삭제, 본인 비밀번호 변경 추가. 로그인 화면에서 데모 계정 표시 제거.

## 2026-07-23
- 최초 커밋: 재고·판매·구매·고객·미수금 관리 앱.
- 데드 코드 제거, 인증/CORS 강화, Vercel 올인원 배포 설정.
